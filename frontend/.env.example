# Base URL for the backend API
# For local development, leave this empty to use the Vite proxy (avoids CORS issues).
# For production (Vercel), set this to your deployed backend URL (e.g., https://my-backend-api.onrender.com).
# Ensure your backend's CORS settings allow requests from your Vercel frontend domain.
VITE_API_BASE_URL=
